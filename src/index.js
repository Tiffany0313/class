import { StrictMode } from "react";
import ReactDOM from "react-dom";

import App from "./App";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <StrictMode>
    <App />
  </StrictMode>,
  rootElement
);

// 1. 完成Main、SignUp、SignOut的修改

// 2. 完成其他相關元件，如ProductList的修改
